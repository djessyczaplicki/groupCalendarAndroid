<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App.SplashTheme" parent="Theme.SplashScreen">
    // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">@color/blue</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_calendar_icon</item>

        <item name="android:windowSplashScreenBrandingImage">@drawable/ic_calendar_icon</item>

        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/Theme.AppCompat</item>
    </style>
</resources>